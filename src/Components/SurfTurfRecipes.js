import React from "react";
import { View, Text, StyleSheet, Button } from "react-native";

const SurfTurfRecipes = ({}) => {
    return <View>
        <Text>Surf & Turf Recipes</Text>
    </View>
};

const styles = StyleSheet.create({

});

export default SurfTurfRecipes;